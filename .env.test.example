# Integration Test Configuration
# Copy this file to .env.test.local and fill in your test credentials
# .env.test.local is gitignored and will not be committed

# ==========================================
# LDAP / Active Directory Configuration
# ==========================================

# LDAP Server URL (ldaps:// recommended)
TEST_LDAP_URL=ldaps://your-dc.domain.com:636

# Service account credentials for testing
TEST_LDAP_BIND_DN=CN=service-user,CN=Users,DC=domain,DC=com
TEST_LDAP_BIND_PASSWORD=your-password

# Base DN for searches
TEST_LDAP_BASE_DN=DC=domain,DC=com

# TLS certificate verification (false for self-signed certs)
TEST_LDAP_TLS_VERIFY=false

# ==========================================
# Azure AD / Microsoft Graph Configuration
# ==========================================

# Azure AD Tenant ID
TEST_AZURE_TENANT_ID=your-tenant-id

# Azure AD Application (Client) ID
TEST_AZURE_CLIENT_ID=your-client-id

# Azure AD Client Secret
TEST_AZURE_CLIENT_SECRET=your-client-secret
